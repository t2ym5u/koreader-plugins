local _ = require("gettext")

return {
    name        = "binairo",
    fullname    = _("Binairo"),
    description = _("Binary grid puzzle — fill each row and column with equal 0s and 1s, no three in a row."),
    version     = "1.0.0",
}
